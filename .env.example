# Application Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Database
DB_PATH=./data/whatsapp.db

# JWT Authentication
# Generate a secure random string (minimum 32 characters)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-long-please-change-this
JWT_EXPIRES_IN=7d

# Encryption
# Generate a 64-character hex string: openssl rand -hex 32
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Rate Limiting
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=info

# ============================================
# PRODUCTION ENVIRONMENT VARIABLES
# ============================================
#
# For production, make sure to:
# 1. Change JWT_SECRET to a strong random string
# 2. Generate a new ENCRYPTION_KEY using: openssl rand -hex 32
# 3. Set appropriate CORS_ORIGIN for your domain
# 4. Use LOG_LEVEL=warn or LOG_LEVEL=error
# 5. Never commit the actual .env file to version control
#
# Example production values:
# NODE_ENV=production
# JWT_SECRET=<generate-strong-random-secret>
# ENCRYPTION_KEY=<generate-using-openssl-rand-hex-32>
# CORS_ORIGIN=https://yourdomain.com
# LOG_LEVEL=warn
